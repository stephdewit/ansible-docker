argument_specs:
  main:
    options:
      docker_tcp_port:
        type: "int"
      docker_tcp_ca:
        type: "str"
      docker_tcp_cert:
        type: "str"
      docker_tcp_key:
        type: "str"
