docker_apt_repository:
  arch: amd64
  gpg_key_url: https://download.docker.com/linux/debian/gpg
  url: https://download.docker.com/linux/debian
  distribution: trixie
  components: stable
docker_configuration_directory: /etc/docker
docker_ssl_directory: "{{ docker_configuration_directory }}/ssl"
docker_systemd_unit_directory: /etc/systemd/system/docker.service.d
