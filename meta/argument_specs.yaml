argument_specs:
  main:
    options:
      docker_tcp_port:
        type: "int"
        required: true
      docker_tcp_cert:
        type: "str"
        required: true
      docker_tcp_key:
        type: "str"
        required: true
